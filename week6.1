#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>


int findnumber(int x,int y) {
	if (x > y) {
		return 0;
	}
	if (y > x) {

		return 1;
	}
}

int main() {
	int x, y;
	printf("enter num1:");
	scanf("%d", &x);
	printf("enter num2:");
	scanf("%d", &y);
	
	if (findnumber(x, y) == 1)printf("%d more than %d",y,x);
	else
		printf("%d more than %d",x,y);
	return 0;
}
