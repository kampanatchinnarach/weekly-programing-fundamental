#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int x, y,A,B,C;

	printf("enter your number1:");
	scanf("%d", &x);
	printf("enter your number2:");
	scanf("%d", &y);

	x - y = A;
	y + x = B;
	y* x = C;

	if (x > y)
	{
		printf("%d", A);
	}
	else if (y > x)
	{
		printf("%d", B);

	}
	else if (y = x)
	{
		printf("%d", C);
	}


	return 0;
}
