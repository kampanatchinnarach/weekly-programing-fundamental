#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
		int x, i=0, y, k;
		scanf("%d",&x);
	do {

		for (k = 0; k < x - i; k++) {
			printf(" ");
			
		}
		for (y = 0; y < i * 2 + 1; y++) {
			printf("*");
		}
		i++;
		printf("\n");
	} while (i < x);
}
