#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<Windows.h>



void onecallaway(int a, int b, int c, int d, int e) {
	
	if (a > b && a > c && a > d && a > e) {
		printf("the most number is %d", a);
	}
	else if(b>a&&b>c&&b>d&&b>e)
	{
		 printf("the most number is %d", b);

	}
	else if (c > a && c > b && c > d && c > e)
	{
		printf("the most number is %d", c);
	}
	else if (d > a && d > b && d > c && d > e) {
		printf("the most number is %d", d);

	}
	else if(e>a&&e>b&&e>c&&e>d)
	{
		printf("the most number is %d", e);
	}
	
}

int main() {
	int h, i, j,k, l;
	scanf_s("%d", &h);
	scanf_s("%d", &i);
	scanf_s("%d", &j);
	scanf_s("%d", &k);
	scanf_s("%d", &l);
	onecallaway(h,i,j,k,l);


}
